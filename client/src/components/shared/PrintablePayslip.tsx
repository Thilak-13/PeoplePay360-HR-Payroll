export { PrintablePayslip, default } from '../../pages/dashboard/PrintablePayslip';
export type { PrintablePayslipProps, PrintableData as PrintablePayslipData } from '../../pages/dashboard/PrintablePayslip';
/* Keep backward-compatible legacy interface aliases */
export interface PayslipItemLine {
  name: string;
  amount: number;
}

